<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Background color -->
    <stroke
        android:width="10dp"
        android:color="@android:color/darker_gray" /> <!-- Border color and thickness -->
    <corners android:radius="0dp" /> <!-- No rounded corners -->
</shape>
